# Build output
build/
*.ipa

# macOS
.DS_Store
*.swp
*~

# Xcode (if ever needed)
*.xcworkspace
*.xcodeproj/

# SwiftPM
.build/
Package.resolved

# Build artifacts / releases
dist/
build/

# Stray compiled binaries from one-off swiftc harnesses (repo root)
/main
*.o
a.out

# Claude Code local working directory (worktrees, local settings)
.claude/

# MAK-65 dev app package (SwiftPM incremental / LSP for the app target)
AppPackage/.build/
AppPackage/Sources/OpenWhispApp/BuildInfo.swift
